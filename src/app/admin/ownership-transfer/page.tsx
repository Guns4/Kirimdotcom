import OwnershipTransferDashboard from '@/components/admin/OwnershipTransferDashboard';

export const metadata = {
    title: 'Ownership Transfer - Admin | CekKirim',
    description: 'Secure platform ownership transfer protocol.',
    robots: 'noindex, nofollow',
};

export default function OwnershipTransferPage() {
    return <OwnershipTransferDashboard />;
}
